
# SlotSwapper Backend (Simple)

## Setup

```
cd backend
npm install
npm run migrate
npm start
```

Server runs on port 4000 by default. Set environment variables:
- JWT_SECRET (optional)
- SQLITE_FILE (optional) to set database path
